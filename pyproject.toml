[project]
name = "ternarycore-tools"
version = "0.1.0"
description = "BitNet ternary accelerator tooling"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0",
    "transformers>=4.40",
    "onnx>=1.14",
    "numpy>=1.24",
]

[tool.uv]
dev-dependencies = []